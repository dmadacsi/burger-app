<li>
	{{name}}

	<button class="change-state" data-id="{{id}}" data-newstate="{{devoured}}">
		{{#if devoured}}DEVOURED{{else}}EAT ME UP!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
